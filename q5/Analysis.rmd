---
title: "Garbage Collector Impact"
author: "David Ferreira Quaresma (david.quaresma@ccc.ufcg.edu.br)"
date: "janeiro, 2019"
output: pdf_document
---

```{r setup, include=FALSE}
require(dplyr)
require(ggplot2)
source("functions.R")
```

# Resultados
```{r}
concurrentHash = read.csv("./results/concurrentHash-t1024-r0.5.csv", header=T, dec=".")
synchronizedMap = read.csv("./results/synchronizedMap-t1024-r0.5.csv", header=T, dec=".")
copyOnWriteList = read.csv("./results/copyOnWriteList-t1024-r0.5.csv, header=T, dec=".")
synchronizedList = read.csv("./results/synchronizedList-t1024-r0.5.csv, header=T, dec=".")
```

## ECDF
```{r}
graph_tail(concurrentHash$time_in_nanoseconds, synchronizedMap$time_in_nanoseconds,
        copyOnWriteList$time_in_nanoseconds, synchronizedList$time_in_nanoseconds,
        title="ECDF", x_limit_inf=0, x_limit_sup=200, annotate_y=0.90)
```
